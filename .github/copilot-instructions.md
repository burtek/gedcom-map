# GitHub Copilot Instructions

Always follow these repository rules:

- Use conventional commits for all commit messages.
- Install dependencies with Yarn before starting work (`yarn install`).
- Keep `README.md` up to date whenever code or behavior changes.
